import{createServer}from'node:http';import{readFile}from'node:fs/promises';import{generate}from'./listings.mjs';const port=Number(process.env.PORT||4202);createServer(async(req,res)=>{try{if(req.url==='/'&&req.method==='GET'){res.writeHead(200,{'content-type':'text/html; charset=utf-8'});res.end(await readFile(new URL('./index.html',import.meta.url)));return}if(req.url==='/listings.mjs'&&req.method==='GET'){res.writeHead(200,{'content-type':'text/javascript'});res.end(await readFile(new URL('./listings.mjs',import.meta.url)));return}if(req.url==='/api/generate'&&req.method==='POST'){let raw='';for await(const c of req){raw+=c;if(raw.length>12000)throw Error('Input too large')}res.writeHead(200,{'content-type':'application/json'});res.end(JSON.stringify(await generate(JSON.parse(raw))));return}res.writeHead(404);res.end()}catch(e){res.writeHead(400,{'content-type':'application/json'});res.end(JSON.stringify({error:e.message}))}}).listen(port,'0.0.0.0',()=>console.log(`http://0.0.0.0:${port}`));
